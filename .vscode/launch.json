{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Update Collection Prices",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "Build",
            "program": "${workspaceFolder}/bin/Debug/net8.0/invintory-updater.dll",
            "args": ["ucp"],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "internalConsole"
        }
    ]
}
